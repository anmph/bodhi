import { Flower2 } from "lucide-react";

export default function LotusIcon({ size = 52, color = "#C8A96E" }: { size?: number; color?: string }) {
  return <Flower2 size={size} color={color} strokeWidth={1.4} />;
}
