import { Leaf } from "lucide-react";

export default function BodhiLeafIcon({ size = 52, color = "#7BA886" }: { size?: number; color?: string }) {
  return <Leaf size={size} color={color} strokeWidth={1.4} />;
}
